# Orchestration

1. Export or write a connector fixture bundle locally.
2. Run `connector-fixture-story-skill <fixture.json>`.
3. Review blockers for live writes, missing approvals, or unredacted secrets.
4. Attach the Markdown story to a connector review or release-candidate PR.

The skill only reads fixture files and prints local reports.
